import React from 'react';

import logo from '../../../../assets/logo.png';

import styles from './logo.module.css';

const Logo = () => <img src={logo} alt='Logo' className={styles.image} />;

export default Logo;
